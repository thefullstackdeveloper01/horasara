/**
 * Backward-compatible export name retained for API compatibility.
 * The implementation is the bundled JS reference engine, NOT the Swiss
 * Ephemeris library. Use ReferenceJsEphemerisProvider for new code.
 */
export { ReferenceJsEphemerisProvider as SwissEphemerisProvider } from './ReferenceJsEphemerisProvider.js';
