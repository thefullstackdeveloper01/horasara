export const ENGINE_ADAPTER_CONTRACT={version:'11.0',required:['name','version','calculate'],conventionsRequired:true,provenanceRequired:true};
export function validateEngineAdapter(adapter){const errors=[];for(const k of ENGINE_ADAPTER_CONTRACT.required)if(!adapter?.[k])errors.push(`missing ${k}`);if(typeof adapter?.calculate!=='function')errors.push('calculate must be function');return {valid:!errors.length,errors};}
export function runIdenticalInputBenchmark(cases=[],adapters=[]){const rows=[];for(const c of cases){const outputs={};for(const a of adapters){const v=validateEngineAdapter(a);outputs[a.name]=v.valid?(()=>{try{return {result:a.calculate(c.input),status:'PASS'}}catch(e){return {status:'ERROR',error:e.message}}})():{status:'INVALID_ADAPTER',errors:v.errors};}rows.push({id:c.id,input:c.input,conventions:c.conventions,outputs,reference:c.reference||null});}return {version:'11.0',cases:rows.length,engines:adapters.map(a=>a.name),rows,externalEvidenceRequired:true};}
