import { eventCutoffSafe } from './ResearchCorpus.js';
export function evaluatePredictions(cases=[],predict){if(typeof predict!=='function')throw new TypeError('predict required');const rows=[];for(const c of cases){const eligible=(c.events||[]).filter(e=>eventCutoffSafe(e,c.cutoff));const prediction=predict(c);const hit=eligible.some(e=>prediction?.eventTypes?.includes(e.type));rows.push({id:c.id,hit,eligibleEvents:eligible.length});}const hits=rows.filter(r=>r.hit).length;return {cases:rows.length,hits,misses:rows.length-hits,hitRate:rows.length?hits/rows.length:null,rows};}
